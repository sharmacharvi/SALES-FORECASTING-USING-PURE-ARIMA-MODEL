# SALES-FORECASTING-USING-PURE-ARIMA-MODEL
This project demonstrates how to predict future sales with the ARIMA (AutoRegressive Integrated Moving Average) model. The dataset used is train.csv, containing historical daily sales values, evaluate its performance using RMSE, and visualize the predicted vs actual sales.
